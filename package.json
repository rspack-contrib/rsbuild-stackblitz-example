{
  "name": "rsbuild-stackblitz-example",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "export NODE_NO_WARNINGS=1 && rsbuild build",
    "dev": "export NODE_NO_WARNINGS=1 && rsbuild dev",
    "preview": "export NODE_NO_WARNINGS=1 && rsbuild preview"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@rsbuild/core": "^1.5.0",
    "@rsbuild/plugin-react": "^1.3.5",
    "@types/react": "^19.1.11",
    "@types/react-dom": "^19.1.8",
    "typescript": "^5.9.2"
  },
  "pnpm": {
    "supportedArchitectures": {
      "cpu": [
        "wasm32"
      ]
    }
  }
}
